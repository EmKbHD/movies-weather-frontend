import React from "react";

import { useQuery } from "@apollo/client";
import { FETCH_ALL_FAVORITES } from "@/lib/graphql";

const FavoritesList = () => {
  return <div>Favorites Movies</div>;
};

export default FavoritesList;
